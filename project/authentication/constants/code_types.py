# password reset code type
# this code can only confirm the password
# reset, it does not apply to other actions
PASSWORD_RESET_CODE_TYPE: str = "password_reset"

# email verification code type
# this code can only confirm the email
# verification, it does not apply to other actions
EMAIL_VERIFY_CODE_TYPE: str = "email_verify"

# password change code type
# this code can only confirm the password
# change, it does not apply to other actions
PASSWORD_CHANGE_CODE_TYPE: str = "password_change"

# email change code type
# this code can only confirm the email
# change, it does not apply to other actions
EMAIL_CHANGE_CODE_TYPE: str = "email_change"

# account delete code type
# this code can only confirm the account
# delete, it does not apply to other actions
ACCOUNT_DELETE_CODE_TYPE: str = "account_delete"
