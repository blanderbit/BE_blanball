

# the error that means that the final date of the 
# scheduler is less than the start date
SCHEDULER_FINISH_DATE_LESS_THAN_START_DATE: dict[str, str] = {
    "error": "scheduler_finish_date_less_than_start_date"
}
